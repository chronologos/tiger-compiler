let
   var N := 8

   type intArray = array of int

   function printboard() =
     (for i:= 0 to N-1
       do (for j:= 0 to N-1
             do print(if col[i]=j then " 0" else ".");
	   print("\n")
	   )
	   )
     )	

   in
     try(0)
 end